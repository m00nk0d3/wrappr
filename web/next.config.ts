import type { NextConfig } from "next";

// Environment variables: see NEXT_PUBLIC_API_URL in .env.example
// Add rewrites/headers here when proxying the Go API in production.
const nextConfig: NextConfig = {};

export default nextConfig;
